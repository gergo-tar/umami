name: Build and Deploy Umami

on:
  push: 
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on:  ubuntu-latest

    steps:
      - name: Checkout code
        uses:  actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version:  10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build application (full build)
        env:
          NODE_OPTIONS: --max-old-space-size=4096
          DATABASE_URL: "file:ci.db"
          DATABASE_TYPE: "sqlite"
        run:  pnpm run build-docker
        
      - name:  Verify build artifacts
        run: |
          echo "=== Build artifacts ==="
          ls -la .next 2>/dev/null && echo "✓ .next exists" || echo "✗ .next missing"
          ls -la public/script.js 2>/dev/null && echo "✓ public/script.js exists" || echo "✗ public/script.js missing"
          ls -la geo 2>/dev/null && echo "✓ geo exists" || echo "✗ geo missing"
          echo "=== Sizes ==="
          du -sh .next public/script.js geo 2>/dev/null || true

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name:  umami-build-${{ github.sha }}
          retention-days: 30
          include-hidden-files: true
          compression-level: 6
          path: |
            .next/
            public/script.js
            geo/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: umami-build-${{ github.sha }}
          path: build-output

      - name:  Verify downloaded artifacts
        run: |
          echo "=== Downloaded artifacts ==="
          ls -laR build-output
          echo "=== File count ==="
          find build-output -type f | wc -l

      - name: Deploy to Forge via rsync
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.FORGE_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.FORGE_HOST }} >> ~/.ssh/known_hosts
          
          # Deploy artifacts to correct locations
          rsync -avz --delete build-output/.next/ ${{ secrets.FORGE_USER }}@${{ secrets.FORGE_HOST }}:${{ secrets.FORGE_PATH }}/.next/
          rsync -avz build-output/geo/ ${{ secrets.FORGE_USER }}@${{ secrets.FORGE_HOST }}:${{ secrets.FORGE_PATH }}/geo/
          rsync -avz build-output/public/ ${{ secrets.FORGE_USER }}@${{ secrets.FORGE_HOST }}:${{ secrets.FORGE_PATH }}/public/

      - name:  Trigger Forge Deploy Script
        run: curl -X POST "${{ secrets.FORGE_DEPLOY_URL }}"
